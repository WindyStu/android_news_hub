<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.final_test">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_exp"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.example.final_test.NewsItemActivity"
            android:exported="false"
            android:parentActivityName="com.example.final_test.MainActivity" />
        <activity
            android:name="com.example.final_test.LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".NewsDetailsActivity" />
        <activity android:name=".CommentActivity" />
        <activity android:name=".PublishNewsActivity" />
        <activity
            android:name="com.example.final_test.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>